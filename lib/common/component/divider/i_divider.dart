import 'package:flutter/material.dart';

class IDivider extends StatelessWidget {
  const IDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
