import 'package:flutter/material.dart';

class IDottedBorder extends StatelessWidget {
  final Widget dotted;
  const IDottedBorder({Key? key, required this.dotted}) : super(key: key);

  // factory IDottedBorder.

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
